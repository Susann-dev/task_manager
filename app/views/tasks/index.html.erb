<h1>Tasks</h1>
<%= link_to 'New Task', new_task_path %>

  <ul>
    <% @tasks.each do |task| %>
      <li><%= task.name %></li>
      <li><%= task.description %></li>
      <li><%= task.priority %></li>
      <% end %>
  </ul>    
 
 <%= link_to 'New Task', new_task_path %>

